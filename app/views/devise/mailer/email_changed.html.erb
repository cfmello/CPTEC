<p>Olá <%= @email %>!</p>

<% if @resource.try(:unconfirmed_email?) %>
  <p>Seu e-mail será alterado para <%= @resource.unconfirmed_email %>.</p>
<% else %>
  <p>Seu e-mail foi alterado para <%= @resource.email %>.</p>
<% end %>
